"""Utils!"""

__author__: "730480912"

def only_evens(input: list[int]) -> list():
    # returning even numbers from a list
    for number in only_evens:
        if number % 2 == 0:
            new_list1 = number
            return new_list1  
def only_evens(list: int) -> list ():
    # store even numbers 
    evens_list1 = []
    for number in list: 
    # determining even
        if number % 2 == 0:
            evens_list1.append(number)
    # returns evens in list after appending to list
    return evens_list1

def concat(list1: list[int], list2: list[int])-> list[int]: 
    new_list = concat([list1], [list2])
    return new_list


def sub(a_list: list, start: int, end: int) -> list(): 
        return a_list in range(start, end, step = 1)
        



